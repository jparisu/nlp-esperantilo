# Example

!!! note "Under construction"
    Planned contents are listed below.

A step-by-step walkthrough that ties the whole section together:

1. Create a repository.
2. Add files and make the first commit.
3. Inspect the state with `status`, `log` and `diff`.
4. Create a branch and work on it.
5. Merge the branch back and resolve a conflict.
6. Connect the repository to a remote and push.

Every step shows the exact commands and the expected output.
